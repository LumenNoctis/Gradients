int RGBA_ToInt(int r, int g, int b, int a)
{

}

void Int_ToRGBA(int color, int *r, int *g, int *b, int *a)
{
	
}

int blurr_texture(SDL_Rect area, int **g_matrix, SDL_Surface *surf,)
{
	int x;
	int y;

	x = area.x;
	y = area.y;
	while (y < area.h + area.y)
	{
		while (x < area.w + area.x)
		{

		}
	}
}
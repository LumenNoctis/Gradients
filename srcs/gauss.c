#include "lerp.h"

double *gauss(gauss_curve curve, int **area, int a_width, int a_height);
{
	int		i;
	int		n;
	int		div;
	int		total;
	double	*result;

	div = 2 * curve.width *curve.width;
	result = calloc(a_width, sizeof(int *));
	while ()
	{
		result = curve.heigh;
		result = pow()

	}
}
